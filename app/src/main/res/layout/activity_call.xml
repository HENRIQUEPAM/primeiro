<?xml version="1.0" encoding="utf-8"?>
<!--
  Layout da chamada. Botoes de 96dp com rotulo em texto: icone sozinho e
  ambiguo para usuario idoso, "Atender" escrito nao e.
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/call_background">

    <org.webrtc.SurfaceViewRenderer
        android:id="@+id/remote_video"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <org.webrtc.SurfaceViewRenderer
        android:id="@+id/local_video"
        android:layout_width="120dp"
        android:layout_height="160dp"
        android:layout_margin="16dp"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <TextView
        android:id="@+id/peer_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="72dp"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:gravity="center"
        android:textSize="34sp"
        android:textStyle="bold"
        android:textColor="@color/call_text"
        android:shadowColor="#000000"
        android:shadowRadius="8"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <TextView
        android:id="@+id/status"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:gravity="center"
        android:textSize="22sp"
        android:textColor="@color/call_text_secondary"
        app:layout_constraintTop_toBottomOf="@id/peer_name"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <LinearLayout
        android:id="@+id/secondary_controls"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginBottom="32dp"
        app:layout_constraintBottom_toTopOf="@id/primary_controls"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <Button
            android:id="@+id/mute_button"
            style="@style/CallSecondaryButton"
            android:text="@string/mute" />

        <Button
            android:id="@+id/switch_camera_button"
            style="@style/CallSecondaryButton"
            android:layout_marginStart="16dp"
            android:text="@string/switch_camera" />
    </LinearLayout>

    <LinearLayout
        android:id="@+id/primary_controls"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginBottom="56dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <Button
            android:id="@+id/decline_button"
            style="@style/CallDangerButton"
            android:visibility="gone"
            android:text="@string/decline" />

        <Button
            android:id="@+id/answer_button"
            style="@style/CallPositiveButton"
            android:layout_marginStart="32dp"
            android:visibility="gone"
            android:text="@string/answer" />

        <Button
            android:id="@+id/hang_up_button"
            style="@style/CallDangerButton"
            android:text="@string/hang_up" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
