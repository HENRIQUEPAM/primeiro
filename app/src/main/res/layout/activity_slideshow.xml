<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FF000000"
    android:clickable="true"
    android:focusable="true">

    <!-- Dois ImageViews sobrepostos: a foto nova entra por cima da antiga. -->
    <ImageView
        android:id="@+id/image_a"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:contentDescription="@string/photo_description" />

    <ImageView
        android:id="@+id/image_b"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:alpha="0"
        android:contentDescription="@string/photo_description" />

    <!-- Acervo vazio: convite direto para adicionar. -->
    <LinearLayout
        android:id="@+id/empty_state"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="32dp"
        android:visibility="gone">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/no_photos_title"
            android:textColor="#FFFFFFFF"
            android:textSize="28sp"
            android:textStyle="bold"
            android:gravity="center" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="@string/no_photos_hint"
            android:textColor="#FFCCCCCC"
            android:textSize="17sp"
            android:gravity="center" />

        <Button
            android:id="@+id/empty_add_button"
            android:layout_width="wrap_content"
            android:layout_height="72dp"
            android:layout_marginTop="28dp"
            android:paddingStart="32dp"
            android:paddingEnd="32dp"
            android:text="@string/add_photos"
            android:textSize="20sp" />
    </LinearLayout>

    <!-- Controles: aparecem ao toque, somem sozinhos. -->
    <LinearLayout
        android:id="@+id/controls"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        android:background="#CC000000"
        android:padding="20dp"
        android:visibility="gone">

        <TextView
            android:id="@+id/photo_count"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="#FFCCCCCC"
            android:textSize="15sp" />

        <Button
            android:id="@+id/call_button"
            style="@style/SlideshowActionButton"
            android:layout_marginTop="12dp"
            android:text="@string/call_someone" />

        <Button
            android:id="@+id/add_photos_button"
            style="@style/SlideshowActionButton"
            android:text="@string/add_photos" />

        <Button
            android:id="@+id/settings_button"
            style="@style/SlideshowActionButton"
            android:text="@string/slideshow_interval" />

        <Button
            android:id="@+id/privacy_button"
            style="@style/SlideshowActionButton"
            android:text="@string/privacy_title" />
    </LinearLayout>

</FrameLayout>
