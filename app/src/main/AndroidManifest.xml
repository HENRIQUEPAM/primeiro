<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <!-- Permissao de assinatura: so apps assinados com a mesma chave leem. -->
    <permission
        android:name="com.portaretrato.permission.READ_CALL_STATE"
        android:protectionLevel="signature" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.front" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <application
        android:name="com.portaretrato.app.PortaRetratoApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PortaRetratoChamadas">

        <!--
            Tela inicial de verdade: login (silencioso, sem toque nenhum) e,
            em seguida, o porta-retrato. Ver o KDoc de LoginActivity.
        -->
        <activity
            android:name="com.portaretrato.app.ui.LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:screenOrientation="fullSensor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            O porta-retrato propriamente dito. Não exportada: só é alcançada a
            partir da LoginActivity, dentro do próprio app — nenhum outro
            aplicativo tem por que abri-la diretamente. Contatos, privacidade e
            ajustes ficam atrás do botão de menu, a um toque dela.
        -->
        <activity
            android:name="com.portaretrato.app.ui.SlideshowActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:screenOrientation="fullSensor" />

        <activity
            android:name="com.portaretrato.app.call.ui.HomeActivity"
            android:exported="false"
            android:label="@string/contacts_title" />

        <!--
            singleTask + excludeFromRecents: a tela de chamada nunca deve
            empilhar duas instancias nem sobrar no historico depois de desligar.
            showOnLockScreen + turnScreenOn fazem o porta-retrato acender
            sozinho quando alguem liga.
        -->
        <activity
            android:name="com.portaretrato.app.call.ui.CallActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:showOnLockScreen="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:screenOrientation="portrait" />

        <activity
            android:name="com.portaretrato.app.ui.PrivacyActivity"
            android:exported="false"
            android:label="@string/privacy_title" />

        <!-- Não exportada: os rostos do acervo são dado biométrico e nenhum
             outro aplicativo tem por que abrir esta tela. -->
        <activity
            android:name="com.portaretrato.app.ui.PeopleActivity"
            android:exported="false"
            android:label="@string/people_title" />

        <service
            android:name="com.portaretrato.app.call.CallService"
            android:exported="false"
            android:foregroundServiceType="camera|microphone" />

        <service
            android:name="com.portaretrato.app.call.CallMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!--
            Publica call_in_progress para o watchdog/MCU (Secoes 6 e 7.5).
            exported=true por necessidade: o consumidor e outro processo.
            Somente leitura, e o provider recusa insert/update/delete.
        -->
        <provider
            android:name="com.portaretrato.app.call.CallStateProvider"
            android:authorities="com.portaretrato.callstate"
            android:exported="true"
            android:readPermission="com.portaretrato.permission.READ_CALL_STATE" />

    </application>
</manifest>
